# Copied to `.env` in this folder (apps/extension) for local builds.
# Used at build time for the service worker fetch target and manifest host_permissions.

VITE_SHOPFRIEND_API_ORIGIN=http://localhost:3000
